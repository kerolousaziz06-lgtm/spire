# Direct dependencies of the ingest pipeline. Pinned so a scheduled run a
# month from now behaves like today's.
#
# psycopg2-binary was MISSING here for a while: requirements.txt was first
# generated with a grep that listed packages by name and simply omitted
# it. Everything worked locally, where the venv had it installed, and the
# first CI run died on `import psycopg2`. Generated from the imports now,
# not from a remembered list.
requests==2.32.5
psycopg2-binary==2.9.12
yfinance==1.2.0

# Pulled in by the above, pinned for reproducibility.
urllib3==1.26.20
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.19
